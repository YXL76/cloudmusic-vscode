export type PlaylistItem = {
  description: string;
  id: number;
  name: string;
  playCount: string;
  subscribedCount: string;
  trackCount: number;
};

export type PlaylistContent = {
  name: string;
  id: number;
  alia: string;
  arName: string;
};
