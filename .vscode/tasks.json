// See https://go.microsoft.com/fwlink/?LinkId=733558
// for the documentation about the tasks.json format
{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "cargo: update",
      "command": "cargo update"
    },
    {
      "type": "shell",
      "label": "yarn: update",
      "command": "rm -rf yarn.lock && yarn && yarn dlx @yarnpkg/sdks vscode vim"
    }
  ]
}
